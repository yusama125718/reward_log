<% content_for(:title) { 'ユーザー作成' } %>
<% content_for(:back_link) { link_to '←一覧へ戻る', users_path } %>

<h1>ユーザー作成</h1>

<div id="editform" class="p-2">
  <%= render partial: "form", locals: { user: @user, url: users_path } %>
</div>